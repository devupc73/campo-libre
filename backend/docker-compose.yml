services:
  api:
    build: .
    ports:
      - '8000:8000'
    env_file:
      - .env

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: campo_libre
      POSTGRES_USER: campo
      POSTGRES_PASSWORD: campo
    ports:
      - '5432:5432'
